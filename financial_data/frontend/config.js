// config.js
//
// The ONE place the frontend knows about the API's location. Kept
// separate from app.js so that "point the dashboard at a different
// API host/port" (useful when practicing DNS / port / networking
// failures) never means digging through application logic.


const API_BASE_URL = "";
